apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: hello-gke-netpol
  namespace: default
spec:
  podSelector:
    matchLabels:
      app: hello-gke
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - ports:
    - port: 8080
  egress:
  - {}
